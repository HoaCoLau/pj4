<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top shadow-sm">
  <div class="container-fluid"> <a class="navbar-brand" href="<%= (typeof currentUser !== 'undefined' && currentUser && currentUser.role === 'admin') ? '/admin/dashboard' : '/' %>">
        <span data-feather="shopping-bag" class="align-text-bottom me-1"></span>
        PJ4 Shop <%= (typeof currentUser !== 'undefined' && currentUser && currentUser.role === 'admin') ? '<span class="badge bg-primary ms-1">Admin</span>' : '' %>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar" aria-controls="mainNavbar" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="mainNavbar">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if (typeof currentUser === 'undefined' || !currentUser || currentUser.role !== 'admin') { %>
            <li class="nav-item">
              <a class="nav-link <%= locals.path === '/' ? 'active' : '' %>" aria-current="page" href="/">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link <%= locals.path && locals.path.startsWith('/products') ? 'active' : '' %>" href="/products">Products</a>
            </li>
        <% } %>
        </ul>
      <ul class="navbar-nav ms-auto">
           <% if (currentUser) { %>
               <li class="nav-item dropdown">
                 <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    <span data-feather="user" class="align-text-bottom me-1"></span>
                    <%= currentUser.name ? currentUser.name.split(' ')[0] : 'Account' %>
                 </a>
                 <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                   <% if (currentUser.role !== 'admin') { %>
                        <li><a class="dropdown-item" href="/orders">My Orders</a></li>
                        <% } %>
                   <% if (currentUser.role === 'admin') { %>
                       <li><a class="dropdown-item <%= locals.path === '/admin/dashboard' ? 'active' : '' %>" href="/admin/dashboard">Admin Dashboard</a></li>
                   <% } %>
                   <li><hr class="dropdown-divider"></li>
                   <li><a class="dropdown-item" href="/auth/logout">Logout</a></li>
                 </ul>
               </li>
           <% } else { %>
                <li class="nav-item">
                    <a class="nav-link <%= locals.path === '/auth/login' ? 'active' : '' %>" href="/auth/login">Login</a>
                </li>
                <li class="nav-item">
                     <a class="nav-link <%= locals.path === '/auth/register' ? 'active' : '' %>" href="/auth/register">Register</a>
                </li>
           <% } %>
      </ul>
    </div>
  </div>
</nav>