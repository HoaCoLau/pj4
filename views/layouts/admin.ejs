<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin - <%= title %></title>
    {# Link đến file CSS tĩnh #}
    <link rel="stylesheet" href="/css/admin.css">
    {# Thêm link CSS, JS cần thiết cho Admin #}
</head>
<body>
    <header>
        <h1>Admin Panel</h1>
        <nav>
            <a href="/admin">Dashboard</a>
            <a href="/admin/products">Products</a>
            <a href="/admin/categories">Categories</a>
            <a href="/admin/orders">Orders</a>
            <a href="/admin/users">Users</a>
            <span>Xin chào, <%= user ? user.name : 'Admin' %></span>
            <a href="/auth/signout">Đăng xuất</a>
        </nav>
    </header>

    <main>
        {# Hiển thị thông báo thành công/thất bại chung cho Admin #}
        <% if (query && query.success) { %> <p style="color: green;"><%= query.success %></p> <% } %>
        <% if (query && query.error) { %> <p style="color: red;"><%= query.error %></p> <% } %>

        <%- body %> {# Nội dung view cụ thể #}
    </main>

    <footer>
        <p>&copy; 2023 Admin Panel</p>
    </footer>

    {# Thêm các script JS chung cho Admin #}
    {# <script src="/js/admin.js"></script> #}
</body>
</html>