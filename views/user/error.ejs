<%- include('../partials/header', { title: title || 'Lỗi' }) %>

 <h1><%= title || 'Đã có lỗi xảy ra' %></h1>
 <p><%= message %></p>

 <% if (process.env.NODE_ENV === 'development' && typeof error !== 'undefined' && error && error.stack) { %>
    <pre><%= error.stack %></pre>
 <% } %>

 <a href="/">Quay về trang chủ</a>

 <%- include('../partials/footer') %>