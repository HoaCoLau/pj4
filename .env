NODE_ENV=development
PORT=3000

# Database Credentials
DB_HOST=localhost
DB_PORT=3306
DB_USER=root        # Username MySQL của bạn
DB_PASSWORD=       # Mật khẩu MySQL của bạn (để trống nếu không có)
DB_NAME=pj4      # Tên database đã tạo từ file pj4.sql

# JWT Settings
JWT_SECRET=thay_the_bang_mot_key_bi_mat_rat_manh_va_dai # RẤT QUAN TRỌNG: Thay đổi key này!
JWT_EXPIRES_IN=1h
JWT_COOKIE_EXPIRES_IN=86400000 # 1 ngày (ms)

# Session Secret (Có thể không cần nếu chỉ dùng JWT, nhưng để đó cũng không sao)
# SESSION_SECRET=optional_secret_key